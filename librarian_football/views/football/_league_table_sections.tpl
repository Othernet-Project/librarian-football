<%namespace file="/ui/nojs.tpl" name="nojs"/>

<%def name="league_rankings(teams)">
    <section>
        <p>${len(teams)}</p>
    </section>
</%def>

<%def name="league_schedule(fixtures)">
    <section>
        <p>${len(fixtures)}</p>
    </section>
</%def>